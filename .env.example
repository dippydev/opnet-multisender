# Each sub-project has its own .env file:
#
#   scripts/.env   — wallet keys (OPNET_MNEMONIC, WIF_PRIVATE_KEY)
#   server/.env    — backend config (PORT, FRONTEND_URL, RPC_URL)
#   frontend/.env  — Vite vars (VITE_API_BASE_URL, VITE_RPC_URL, etc.)
#
# See each folder's .env.example for details.
# Contracts (contracts/) do not need any env vars.
